export FLASK_SECRET=
export FLASK_ENV=
export PYTHONPATH=`pwd`
export FLASK_PORT=5050

export PSQL_USERNAME=
export PSQL_PASSWORD=
export PSQL_HOST='127.0.0.1'
export PSQL_PORT='5432'

export BONNIEDOTDEV_LOGDIR=logs
export BONNIEDOTDEV_LOGNAME=app.log

# assume this starts with at the app directory
export BDD_UPLOAD_FOLDER="static/images/uploaded"

# for the log -- do not edit
mkdir $BONNIEDOTDEV_LOGDIR >& /dev/null
touch $BONNIEDOTDEV_LOGDIR/$BONNIEDOTDEV_LOGNAME
